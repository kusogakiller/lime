// Iteration 18 corpus: provides the link-anchor definition so the prepared
// artifact links for a struct-only corpus (manifest `symbols` would otherwise
// be empty and `lime build` would skip the store).
int anon_flatten_link_anchor(void) {
    return 0;
}
